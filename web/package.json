{
  "name": "aetherterm-web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outdir=assets --entry-names=app --legal-comments=linked",
    "check": "node --check src/app.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
